# Tool Examples

Copy-paste examples for all 19 MCP tools. All parameters shown are the tool input JSON.

## Read Tools

### memory_help

```json
{}
```

### memory_status

```json
{ "projectRoot": "/path/to/project" }
```

### memory_search

```json
{
  "query": "authentication",
  "limit": 10,
  "includeContent": true,
  "tags": ["security"],
  "types": ["decision", "architecture"],
  "staleOnly": false
}
```

### memory_get_bundle

```json
{
  "prompt": "Implementing user authentication with JWT",
  "maxItems": 12,
  "maxChars": 6000,
  "includePinned": true,
  "types": ["decision", "constraint"]
}
```

## Write Tools

### memory_save

```json
{
  "title": "Use JWT for API authentication",
  "content": "Chose JWT over session cookies for stateless auth. Tokens expire after 24h.",
  "type": "decision",
  "tags": ["auth", "api"],
  "pinned": true,
  "expiresAt": "2026-06-01T00:00:00Z",
  "supersede": false
}
```

With `supersede: true`, if a similar item exists, the most similar one is archived automatically.

### memory_pin

```json
{
  "itemId": "mem_abc123def456",
  "pinned": true
}
```

### memory_update

```json
{
  "itemId": "mem_abc123def456",
  "title": "Updated title",
  "content": "Updated content",
  "tags": ["new-tag"]
}
```

### memory_delete

```json
{
  "itemId": "mem_abc123def456"
}
```

## Proposal Tools

### memory_propose

```json
{
  "items": [
    {
      "title": "Migrate to PostgreSQL",
      "content": "SQLite won't scale for multi-user. Switch to PostgreSQL.",
      "type": "decision",
      "tags": ["database"]
    }
  ],
  "reason": "Database scaling discussion"
}
```

### memory_list_proposals

```json
{
  "status": "pending",
  "includeContent": true,
  "limit": 20
}
```

### memory_approve_proposal

```json
{
  "proposalId": "prop_abc123def456",
  "action": "approve",
  "edits": {
    "tags": ["database", "migration"]
  }
}
```

## Compact & Archive Tools

### memory_compact

```json
{
  "maxItems": 50,
  "summaryTitle": "Archive batch - March 2026",
  "summaryMaxEntries": 20,
  "summaryTags": ["archive"]
}
```

### memory_search_archive

```json
{
  "query": "database migration",
  "limit": 10,
  "includeContent": false
}
```

### memory_restore

```json
{
  "itemId": "mem_abc123def456"
}
```

## Suggestion Tools

### memory_observe

```json
{
  "observations": [
    {
      "type": "bash_output",
      "content": "npm install express@4.18.2",
      "timestamp": "2026-03-02T10:00:00Z"
    },
    {
      "type": "error",
      "content": "TypeError: Cannot read property 'id' of undefined"
    }
  ]
}
```

### memory_suggest

```json
{}
```

Returns pending suggestions generated by the engine.

### memory_suggestion_feedback

```json
{
  "suggestionId": "sug_abc123def456",
  "action": "accept"
}
```

## Config Tools

### memory_configure_suggestions

```json
{
  "notifyThreshold": 3,
  "autoSaveThreshold": 5,
  "autoSaveEnabled": true
}
```

Overrides persist in `.ai/memory-mcp.json`.

## Export Tools

### memory_export

```json
{
  "format": "markdown",
  "includeArchived": true,
  "types": ["decision", "architecture"],
  "tags": ["api"]
}
```

Formats: `json` (raw array) or `markdown` (grouped by type with metadata).
