{
  "name": "project-memory-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Project-scoped centralized memory as an MCP stdio server (JSON file backend).",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
